import Lean4Lean.Theory.Typing.Lemmas

/-! # Parked stronger-environment prototype

This abandoned environment redesign predates registered structure eta,
contains its own admissions, and has no consumers.  It is intentionally
parked as an import-compatible stub instead of being adapted into the
L4L-16 trust path.
-/
