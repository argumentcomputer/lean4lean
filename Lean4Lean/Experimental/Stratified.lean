import Lean4Lean.Theory.Typing.Lemmas
import Lean4Lean.Theory.Typing.Strong

/-! # Parked stratification prototype

This pre-L4L-15 prototype targeted the old, non-mutual `IsDefEq` judgment
and was never imported by a supported root.  It is intentionally parked as
an import-compatible stub; the maintained stratification development lives
in `Theory.Typing.Strong`, and the semantic route used by L4L-16 lives in
`Experimental.SExpr` and `Experimental.ShapeLogRelAdequacy`.
-/
